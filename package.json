{
  "name": "job-app-client",
  "description": "Simple discord bot as mcp client to be used for MCP demo.",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun run --hot src/index.ts",
    "install:production": "bun install --production"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "discord.js": "^14.19.3",
    "zod": "^3.24.4"
  }
}
